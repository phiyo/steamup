import React from 'react';
import Layout from '../components/layout/Layout';

const NotFound = () => (
  <Layout>
    <h1>Not Found</h1>
    <p>You just hit a page that may have been eaten by...</p>
  </Layout>
);

export default NotFound;
